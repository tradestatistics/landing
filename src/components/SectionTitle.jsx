import React from "react";

const SectionTitle = ({ text }) => (
	<div className="ts-section-title padding-2">
		<h4 className="title">{ text }</h4>
		<div className="underline" />
	</div>
)

export default SectionTitle;