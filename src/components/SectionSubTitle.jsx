import React from "react";

const SectionSubTitle = ({ text }) => (
	<div className="ts-section-subtitle padding-2">
		<h5 className="title">{ text }</h5>
		<div className="underline" />
	</div>
)

export default SectionSubTitle;
